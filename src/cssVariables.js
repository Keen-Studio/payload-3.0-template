// Keep these in sync with the CSS variables in the `_css` directory

const cssVariables = {
  breakpoints: {
    xs: 400,
    s: 768,
    m: 1024,
    l: 1279,
    xl: 1679,
    xxl: 1920
  }
};

export default cssVariables;
